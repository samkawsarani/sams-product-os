id: orphan_task
name: Orphan task with no goal alignment
description: Agent receives a task that doesn't align with any goal in GOALS.md.
user_message: |
  Process this backlog item:

  ```
  - Reorganize office supplies and restock printer paper
  ```
criteria:
  - name: flags_no_goal
    description: Agent flags that this task doesn't align with any current goal in GOALS.md
  - name: asks_about_alignment
    description: Agent asks the user about goal alignment or why this matters
fail_if_present: []
pass_threshold: 1.0
